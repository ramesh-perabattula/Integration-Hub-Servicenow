// Business rule has been DELETED. 
// We now use Dynamic REST messages directly in ZoomIntegrationHelper.js
// This eliminates the need for sys_rest_message records entirely and resolves all cross-scope privilege errors.